public class Main {
    public static void main(String[] args ) {
        // variable = a reusable container for valuable
        //            a variable behave as it was the value it contains

        // primitive = simple value in stored directly in memory ( stack )
        // reference = memory address ( stack ) the points to the ( heap )

        // primitive vs reference
        // ---------    ---------
        // int          string
        // double       array
        // char         object
        // boolean

        // 2 steps to creating variable
        // ----------------------------
        // 1. declaration
        // 2. assignment

        int age = 5;
        int year = 2025;
        int zodiac = 10;

        // System.out.print(age);

        double GPA = 79.9;
        double price = 275.9;
        double temperature = 33.3;

        // System.out.print(GPA);

        char grade = 'A';
        char symbol = '!';
        char currency = '$';

        // System.out.print(grade);

        boolean isStudent = true;
        boolean isHuman = true;

        System.out.print(isStudent);
    }
}
